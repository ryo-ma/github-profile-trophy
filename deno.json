{
  "tasks": {
    "start": "deno run -A main.ts",
    "debug": "deno run --inspect-brk -A main.ts",
    "format": "deno fmt",
    "lint": "deno lint",
    "test": "ENV_TYPE=test deno test --allow-env"
  }
}
